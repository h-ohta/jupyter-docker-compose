version: '2.2'
services:
  jupyterlab:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8888:8888
    volumes:
      - ./work:/home/jovyan/work:rw
      - ./.local:/home/jovyan/.local:rw
      - ./.nbconfig:/home/jovyan/.jupyter/nbconfig:rw
    command: start.sh jupyter lab --NotebookApp.token=''
    restart: always
 
